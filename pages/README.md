# Next.js Pages Router Placeholder

이 디렉터리는 의도적으로 비워 둔다.

- 이 프로젝트는 루트 `app/` 디렉터리의 Next.js App Router를 사용한다.
- 이 플레이스홀더는 Next.js가 `src/pages/`를 Pages Router 디렉터리로 처리하지 않게 막는다.
  - `src/pages/`는 Feature-Sliced Design pages layer용으로 예약되어 있다.
