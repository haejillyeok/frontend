# Naming Convention

## 브라우저 저장소 키

`localStorage`, `sessionStorage` 같은 브라우저 저장소 키는 서비스명
`haejillyeok`의 축약형 `hae`를 prefix로 붙이고 kebab-case로 작성한다.

```ts
const loginDataStorageKey = "hae-login-data";
```
